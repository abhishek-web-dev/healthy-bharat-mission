// content.js - Temporarily disabled to preserve the original static HTML layout.
// The dynamic fetching logic was replacing the beautiful static Tailwind grids with basic HTML.

document.addEventListener('DOMContentLoaded', async () => {
    console.log('content.js logic disabled to preserve static design.');
});
